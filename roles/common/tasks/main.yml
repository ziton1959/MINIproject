---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install common packages
  apt:
    name:
      - curl
      - vim
      - ufw
      - python3
    state: present

- name: Set timezone to UTC
  timezone:
    name: UTC
